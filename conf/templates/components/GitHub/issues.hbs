{{#if componentData.list}}
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <p class="text-center home-suggestions-title">Some GitHub issues</p>
                <ul>
                    {{#each componentData.list}}
                        <li class="" style=""><a href="{{url}}">{{name}}</a></li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
{{/if}}
